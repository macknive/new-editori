query DeliverablesByWorkspace($workspaceSlug: String!) {
  deliverables(where: { workspace: { slug: $workspaceSlug } }) {
    id
    title
    slug
    data
    updated_at
    workflow_data {
      assignee {
        display_name
      }
      deadline
      completed
      rejected
      step {
        label
      }
    }
  }
}
